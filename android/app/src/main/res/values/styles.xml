<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Main theme once launched. The green window background covers the gap
         while the WebView loads the app (was @null, which showed black). -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/green_dark</item>
    </style>

    <!-- Launch theme (AndroidX core-splashscreen). The un-prefixed
         windowSplashScreen* attributes are the compat versions — they work on
         every Android version; the android:-prefixed ones are Android 12+ only.
         Android 12+ always shows its own system splash (it can't be disabled),
         and @capacitor/splash-screen keeps it up for launchShowDuration. That
         splash crops its icon to a 192dp circle inside a 288dp square, so
         splash_icon.png is the full logo at 144dp centred on a transparent
         288dp canvas — the circle only ever crops empty space, never the
         gold border. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/green_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
